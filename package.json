{
  "name": "developer.begin.com",
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy",
    "start": "npm run build && ARC_STATIC_SPA=false && sandbox"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.9.0",
    "@architect/sandbox": "^1.4.16"
  }
}
